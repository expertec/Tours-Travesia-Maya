---
import { Pyramid, Bird, Gem, Castle } from 'lucide-react';
import { promises as fs } from 'fs';
import { extname } from 'path';

const galleryDir = new URL('../../public/img/galeria', import.meta.url);
const validExtensions = new Set(['.jpg', '.jpeg', '.png', '.webp', '.avif', '.gif']);
let galleryImages = [];

try {
	const files = await fs.readdir(galleryDir);
	galleryImages = files
		.filter((file) => validExtensions.has(extname(file).toLowerCase()))
		.sort()
		.map((file) => `/img/galeria/${file}`);
} catch (error) {
	console.error('Error leyendo la galería', error);
}
---

<section id="galeria" class="py-20 bg-white">
  <div class="container mx-auto px-4">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">
        Nuestras <span class="text-amber-500">Experiencias</span>
      </h2>
      <p class="text-xl text-gray-600 max-w-2xl mx-auto">
        Descubre los destinos y vehículos que hacen de cada viaje una aventura inolvidable
      </p>
    </div>

    <!-- Gallery Grid -->
    <div id="gallery-grid" class="grid grid-cols-1 gap-6 mb-12 sm:grid-cols-2 md:grid-cols-3">
      {galleryImages.length ? (
        galleryImages.map((image, index) => (
          <button
            type="button"
            key={image}
            data-image={image}
            class="rounded-3xl bg-white p-4 shadow-2xl transition-transform duration-300 hover:-translate-y-1 focus:outline-none focus:ring-4 focus:ring-amber-400/40"
            aria-label={`Abrir imagen ${index + 1}`}
          >
            <img
              src={image}
              alt={`Galería ${index + 1}`}
              class="w-full h-auto object-contain"
              loading="lazy"
              style="display: block;"
            />
          </button>
        ))
      ) : (
        <p class="text-center text-gray-500 col-span-full">
          No se encontraron imágenes en <code class="font-mono">public/img/galeria</code>. Agrega archivos JPG/PNG/WEBP para poblar esta sección.
        </p>
      )}
    </div>

    <div
      id="gallery-lightbox"
      class="hidden fixed inset-0 z-50 items-center justify-center bg-black/70 px-4 py-10"
      aria-hidden="true"
    >
      <div class="relative w-full max-w-5xl rounded-3xl bg-white/0 shadow-2xl">
        <button
          type="button"
          data-close
          class="absolute top-4 right-4 inline-flex h-10 w-10 items-center justify-center rounded-full bg-white/80 text-gray-900 hover:bg-white focus:outline-none"
          aria-label="Cerrar vista ampliada"
        >
          &times;
        </button>
        <img id="gallery-lightbox-img" src="" alt="Imagen ampliada" class="w-full h-auto rounded-3xl object-contain" />
      </div>
    </div>

    <!-- Popular Destinations -->
    <div class="bg-gradient-to-br from-gray-50 to-amber-50 rounded-2xl p-8 md:p-12">
      <h3 class="text-3xl font-bold text-gray-900 mb-8 text-center">Destinos Populares</h3>

      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
        <div class="bg-white rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2">
          <div class="flex justify-center mb-3">
            <Pyramid className="w-12 h-12 text-amber-500" client:load />
          </div>
          <h4 class="text-xl font-bold text-gray-900 mb-2">Chichén Itzá</h4>
          <p class="text-gray-600 text-sm">Maravilla del mundo moderno</p>
        </div>

        <div class="bg-white rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2">
          <div class="flex justify-center mb-3">
            <Bird className="w-12 h-12 text-pink-500" client:load />
          </div>
          <h4 class="text-xl font-bold text-gray-900 mb-2">Celestún</h4>
          <p class="text-gray-600 text-sm">Reserva de flamencos rosados</p>
        </div>

        <div class="bg-white rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2">
          <div class="flex justify-center mb-3">
            <Gem className="w-12 h-12 text-purple-500" client:load />
          </div>
          <h4 class="text-xl font-bold text-gray-900 mb-2">Uxmal</h4>
          <p class="text-gray-600 text-sm">Ciudad maya patrimonio UNESCO</p>
        </div>

        <div class="bg-white rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2">
          <div class="flex justify-center mb-3">
            <Castle className="w-12 h-12 text-yellow-600" client:load />
          </div>
          <h4 class="text-xl font-bold text-gray-900 mb-2">Izamal</h4>
          <p class="text-gray-600 text-sm">Pueblo mágico amarillo</p>
        </div>
      </div>

      <div class="mt-8 text-center">
        <p class="text-gray-700 mb-4 text-lg">
          <strong>¡Y muchos destinos más!</strong> Arma tu propio tour personalizado
        </p>
        <a href="#contacto" class="inline-flex items-center bg-amber-500 hover:bg-amber-600 text-white px-8 py-3 rounded-full font-semibold transition-all duration-300 transform hover:scale-105">
          Planifica Tu Tour
          <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
          </svg>
        </a>
      </div>
    </div>

    <!-- Instagram CTA -->
    <div class="mt-12 text-center bg-gradient-to-r from-purple-500 to-pink-500 rounded-2xl p-8 text-white shadow-xl">
      <h3 class="text-2xl font-bold mb-4">¡Síguenos en Instagram!</h3>
      <p class="mb-6">Descubre más fotos y experiencias de nuestros clientes</p>
      <a
        href="https://www.instagram.com/tours_travesia_maya/"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center bg-white text-purple-600 px-8 py-3 rounded-full font-semibold transition-all duration-300 transform hover:scale-105"
      >
        <svg class="w-6 h-6 mr-2" fill="currentColor" viewBox="0 0 24 24">
          <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
        </svg>
        @tours_travesia_maya
      </a>
    </div>
 </div>
</section>

<script type="module" client:load>
  const galleryGrid = document.getElementById('gallery-grid');
  const lightbox = document.getElementById('gallery-lightbox');
  const lightboxImage = document.getElementById('gallery-lightbox-img');

  const openLightbox = (src) => {
    if (!lightbox || !lightboxImage) return;
    lightboxImage.src = src;
    lightbox.classList.remove('hidden');
    lightbox.removeAttribute('aria-hidden');
    document.body.dataset.galleryOpen = 'true';
    document.body.classList.add('overflow-hidden');
  };

  const closeLightbox = () => {
    if (!lightbox || !lightboxImage) return;
    lightbox.classList.add('hidden');
    lightbox.setAttribute('aria-hidden', 'true');
    lightboxImage.src = '';
    document.body.removeAttribute('data-gallery-open');
    document.body.classList.remove('overflow-hidden');
  };

  galleryGrid?.addEventListener('click', (event) => {
    const trigger = event.target.closest('[data-image]');
    if (!trigger) return;
    openLightbox(trigger.dataset.image);
  });

  lightbox?.addEventListener('click', (event) => {
    if (event.target === lightbox || event.target.dataset.close !== undefined) {
      closeLightbox();
    }
  });

  document.addEventListener('keydown', (event) => {
    if (event.key === 'Escape') {
      closeLightbox();
    }
  });
</script>
